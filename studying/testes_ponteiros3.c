#include <stdio.h>
#include <stdlib.h>

int teste() {
  int *v;
  int i, num_componentes;
  ptr = (int*) malloc(num_componentes * sizeof());




  printf("Informe o numero de componentes do vetor\n");
  scanf("%d", &num_componentes);

  for (i = 0; 1 < num_componentes; i++)
    {
      scanf("%d", ptr + i);
    }

  return 0;
}